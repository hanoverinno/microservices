---
# Name and port at ususal
spring:
  application:
    name: setting
    # configuration server uri
  cloud:
    config:
      uri:
      - http://localhost:9999
        sleuth:    sampler:      rate: 100
  
  zipkin:
    baseUrl: http://localhost:9411     sender:      type: rabbit
#rabbitmq
#  jackson:#    serialization:#      fail-on-empty-beans:  false      
#logging:#  level:
#    org.springframework.web.servlet.DispatcherServlet: debug#    com.netflix.config.sources.URLConfigurationSource: debug
  
      
 
  