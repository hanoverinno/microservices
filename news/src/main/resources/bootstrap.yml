---
# Name and port at ususal
server:  port: 6001
  
spring:
  application:
    name: news
    # configuration server uri
  cloud:
    config:
      uri:
      - http://localhost:9999        sleuth:    sampler:      rate: 50
  
  zipkin:
    sender:      type: web  jpa:    show-sql: true
      
 
  